
const initialState = [
  {
    title: 'Profile Picture',
    name: 'ProfilePicture'
  },
  {
    title: 'Skills',
    name: 'SkillForm'
  }
];

export default (state = initialState, action) => {
  switch (action.type) {
    default: return state;
  }
}
